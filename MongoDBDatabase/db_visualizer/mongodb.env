# MongoDB container environment configuration for local development
# Port binding used across the stack is 5001.
MONGO_PORT=5001

# Optional placeholders for typical Mongo settings (uncomment and set if needed)
# MONGO_INITDB_ROOT_USERNAME=
# MONGO_INITDB_ROOT_PASSWORD=
# MONGO_INITDB_DATABASE=device_inventory

# Notes:
# - Ensure your Docker compose or container runtime maps host port 5001 to the container MongoDB port (typically 27017).
#   Example (docker run): -p 5001:27017
# - Backend MONGODB_URI should use mongodb://localhost:5001
